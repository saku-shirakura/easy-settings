フィールドのタイプをキーから取得します。

取得される値は以下のようになります。(フィールドは、レジストリの定義における構造体のフィールド型のことです。)

|       | 値                     |
|-------|-----------------------|
| フィールド | `Option<i64>`         |
| 戻り値   | `TypeId::of::<i64>()` |