The settings are saved to the database.

The settings are not applied.

If you haven't performed any operations other than [`SettingManager::get_registry()`], you can apply the settings by calling [`SettingManager::apply()`].