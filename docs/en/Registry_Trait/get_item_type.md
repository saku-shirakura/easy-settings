This function retrieves the field type from the key.

The retrieved value will be as follows: (A field is a field type in a structure within a registry definition.)

|        | Value                 |
|--------|-----------------------|
| Field  | `Option<i64>`         |
| Return | `TypeId::of::<i64>()` |